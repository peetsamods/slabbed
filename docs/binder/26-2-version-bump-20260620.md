# 26.2 version bump (2026-06-20)

`scope: release metadata` · `status: recorded` · `branch: port/mc-26.2-0.4.1-beta.1`

## Why this note exists

The 26.2 P26 code and live/manual queue were green, but the first proof-only hygiene pass for the proposed
`0.4.2-beta.1+26.2` target correctly stopped because the rebuilt jar still identified as
`0.4.1-beta.1+26.2-port`.

## Current truth recorded

- P26 live-green savepoint: `689a8196`
- Save tag: `save/port-26-2-0-4-1-beta-1-p26-live-green`
- Intended next release target: `0.4.2-beta.1+26.2`
- Metadata change: `gradle.properties` now sets `mod_version=0.4.2-beta.1+26.2`

## Evidence status

The pre-bump hygiene evidence lives in `tmp/pre-release-hygiene-689a8196/`. It passed compile, `runGameTest`,
clean build, jar-content scan, and hard-reference scan, then stopped only because the release jar still carried the old
version string.

## Open / next

Rerun `$slabbed-pre-release-hygiene` from the version-bumped commit. If it returns A, use a separate authorized release
slice for any release-candidate/version tag and upload work.
