# MC 26.1.2 Rearm Revert and Release Re-Certification

Status: **release-candidate restored; chained-placement gap deferred pending deterministic RED**.

## Scope

This note records the 2026-05-31 Opus handoff findings for the `port/mc-26.1.2` checkout at HEAD `a1037642` / tag `save/port-26.1.2-dy0-slab-shape-dispatch`.

It is documentation-only. It does not create a new savepoint, release tag, or source change.

## Findings

- The dirty "after-consume rearm" WIP changed `PlacementIntentState.java`, `BlockItemPlacementIntentMixin.java`, and `GameRendererCrosshairRetargetMixin.java`.
- Its core behavior was client clear deferral plus a one-shot rearm permit after `AFTER_CONSUME`.
- The rearm WIP improved the chained lowered-side-slab live route from `placementExpectedDyMismatchRows=4` to `0`.
- The same WIP introduced `outlineRaycastSplitRows=5999`, all at clicked owner `20,-58,24`, which is a cursor outline/raycast split on the route being tested.
- Root cause classification: deferring the client clear lets the server consume the same placement-intent snapshot that HEAD does not expose server-side. That desyncs the authoritative placement result from the client-rendered outline and is unsafe for multiplayer because the client-side snapshot can leak beyond the server consume point.

Rejected path:

- Do not retry deferred client clear / after-consume rearm as the next fix.
- Do not blind-patch the remap result type without a deterministic RED.

Archived WIP:

```text
tmp/reverted-rearm-wip-20260531-161302/
```

Restore command if explicitly needed:

```bash
git apply tmp/reverted-rearm-wip-20260531-161302/rearm-wip-3file.diff
```

## Restored State

The three source files were restored to HEAD `a1037642`. The release-candidate source state is the existing committed/tagged savepoint:

```text
save/port-26.1.2-dy0-slab-shape-dispatch
```

Tracked doc edits may remain dirty after handoff updates. Source state must be verified with Git before source work.

## Re-Certification Evidence

Evidence directory:

```text
tmp/release-cert-20260531-161833/
```

Observed green gates from the handoff:

- `compileJava compileClientJava compileGametestJava`: `BUILD SUCCESSFUL`
- runner3: `RUNNER3_SUMMARY rows=260 placeRows=260 traceMismatches=44 placeMismatches=44 mismatches=88`
- hitbox gate: `raycastCoLocated=yes` and `collisionCoLocated=yes`, with `CoLocated=no` count `0`
- default `runClientGameTest`: `BUILD SUCCESSFUL`
- focused dy15: `classification=GREEN_LEGAL_LOWERED_SIDE_SLAB_PLACEMENT_AUTHORING`
- focused dy15 critical counts: `liveFailUnauthorizedDyRows=0`, `renderBridgeMismatchRows=0`, `targetBlockNoSurfaceReplayRows=0`, `hiddenOwnerPlacementRows=0`, `loweredSideSlabPlacementVanillaDyRows=0`, `legalLoweredSlabSurfaceMismatchRows=0`
- recorder contract: `BUILD SUCCESSFUL`
- `git diff --check`: clean in the certified restored state

Release build evidence:

- `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew --no-daemon clean build --console plain` returned `BUILD_EXIT=0`.
- Runtime jar: `build/libs/slabbed-0.2.0-beta.4+26.1.2-port.jar`.
- Sources jar: `build/libs/slabbed-0.2.0-beta.4+26.1.2-port-sources.jar`.
- Documented leakage scan passed for forbidden release artifacts: no `runner3`, `gametest`, `probe`, `trace`, `debug`, `provenance`, or `/tmp/` payloads.
- `LiveCursorIntentRecorder` is intentionally present in runtime and sources jars because shipping mixins reference it. It is gated off by default with `slabbed.liveCursorIntentRecorder`; it is not a release hygiene leak.

## Residual Classification

`legalObjectTriadMismatchRows=2` is accepted for this savepoint.

The two rows are:

- `dy15_oak_fence_object`
- `dy15_cobblestone_wall_object`

Both are classified as `VALID_NAMED_DYNAMIC_DY15_FENCE_WALL_SUPPORTED_OBJECT`. They represent vanilla-consistent fence/wall collision-taller-than-visual geometry, not a clipping, placement, or dy0 slab dispatch defect. Hitbox proof separately confirmed raycast/collision co-location.

## Open Gap

Aggressive chained lowered-side-slab placement can still author `top/dy0` on the live north-face route:

```text
clicked owner: 20,-58,24
clicked face: north
placement pos: 20,-58,23
held item: minecraft:stone_slab
```

Current classification:

- The correct `placement_intent_visible_lane` branch is gated on a placement-intent snapshot.
- In single-player, the client path can consume/clear that snapshot before the server placement path uses it.
- The server then falls through to the geometric `remap_path`, which can author `minecraft:stone_slab[type=top]` at `dy=0.0`.

This is not a release blocker for the restored savepoint because the full automated suite is green and the rearm WIP failed the no-clipping requirement.

## Next Slice

Build a deterministic gametest RED for the chained lowered-side-slab route above. Only after the RED reproduces should a lifecycle-free fix touch remap branch selection or result-type logic, followed by red-to-green proof and the full release gate suite.
