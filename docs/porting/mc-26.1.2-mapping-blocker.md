# Slabbed MC 26.1.2 Mapping Provider Blocker

Status: **historical blocker; superseded by later compile/build proof on the active port checkout**.

This note preserves the original mapping-provider/tooling stop. It is not the active blocker for HEAD `a1037642` / tag `save/port-26.1.2-dy0-slab-shape-dispatch`; later work compiled and built the port with the values below. Keep this file for provenance and do not use it to stop source slices unless fresh mapping evidence regresses.

## Scope
- Slice objective: record mapping-provider evidence and current proof gaps only.
- Active branch: `port/mc-26.1.2`
- Base release: `release/0.2.0-beta.4`
- Base/head: `f9014fbfcb15af2716f090d038762fd8d3d460de`

No Slabbed Java, gametest, mixin, gameplay, placement, survival, model, outline, raycast, or rescue code was intentionally edited in these mapping-forensics/provenance slices.

## Proven factual state before blocker note
- `release/0.2.0-beta.4` port base is established on
  `f9014fbfcb15af2716f090d038762fd8d3d460de`.
- Slabbed metadata edits have already been made experimentally in this slice sequence:
  - `build.gradle`: Fabric Loom / mappings strategy attempts
  - `gradle.properties`: 26.1.2 metadata
  - `gradle/wrapper/gradle-wrapper.properties`: Gradle wrapper version experiments
  - `src/main/resources/fabric.mod.json`: minecraft dep retarget

This is not a proven final baseline.

## Current experimental values (for evidence only)
- `gradle.properties`
  - `minecraft_version=26.1.2`
  - `loader_version=0.19.2`
  - `fabric_version=0.145.4+26.1.2`
  - `mod_version=0.2.0-beta.4+26.1.2-port`
  - `yarn_mappings` removed
- `build.gradle`
  - `fabric-loom 1.15.5`
  - explicit `mappings`/`loom.officialMojangMappings()` removed in latest attempt
- `gradle/wrapper/gradle-wrapper.properties`
  - wrapper URL to `gradle-9.4.1-bin.zip`

## Compile/provenance evidence sequence (all attempts failed before Slabbed Java)
1. Baseline 26.1.2 metadata retarget under previous Loom versioning:
   - Observed error: Yarn coordinate missing
   - `Could not find net.fabricmc:yarn:26.1.2+build.1`
2. Official Mojang route under Loom experiments:
   - `Failed to find official mojang mappings for 26.1.2`
3. Loom 1.16.2 under wrapper 9.3.1:
   - plugin variant mismatch (`api-version 9.4.0` vs consumer `9.3.1`)
4. Manual/automatic wrapper pin to 9.4.0 then 9.4.1:
   - no compile-time mapping resolution of official mappings still
5. Proven pattern probe: remove explicit mappings and use Loom 1.15.5 + wrapper 9.4.1:
   - compile error becomes: `Configuration 'mappings' has no dependencies`
6. Latitude read-only forensic follow-up:
   - Latitude checked: `/Users/joolmac/CascadeProjects/Latitude (Globe)` @ `feat/1.3.1-cohesive-horizons-26.1.2` (`f0b1cdb93e40a344d597fe552462218a18d27c61`)
   - `./gradlew --version` works on Java 25, Gradle 9.4.1
   - `compileJava` succeeds
   - `dependencies --configuration mappings` fails in Latitude with `configuration 'mappings' not found`
   - full mapping text search (`mappings`, `officialMojangMappings`, `intermediary`, `yarn`) found no explicit declaration in inspected Latitude refs (`feat/1.3.1-cohesive-horizons-26.1.2`, `origin/...`, `save/cohesive-horizons-bop-26-1-2-pilot`, `5c8ed3e`)

## Original stop condition
The original stop condition was **proven mapping mechanism gap**: no explicit mapping strategy had been established as the mechanism that made Latitude 26.1.2 compile cleanly for our Slabbed transplant.

That stop condition has been overtaken by later local proof in this checkout. The active blocker state now lives in `SLABBED_SPINE.md` and, for the 2026-05-31 release re-certification, `docs/porting/mc-26.1.2-rearm-revert-release-cert.md`.

## Still unproven from this historical slice
- Whether Latitude relies on implicit/default Loom mapping behavior not reflected in searched text windows.
- The exact mapping declaration or plugin-supplied default behavior required for Slabbed to keep mappings configured.
- Whether a verified clean Latitude working snapshot can be obtained for direct file-level confirmation.

## Historical next smallest slice
If the mapping layer regresses again, the clean-forensic continuation remains:
1) confirm a clean Latitude 26.1.2 revision (or another known-good donor) with proven mapping mechanism,
2) then apply **exactly one** explicit mapping declaration only if directly proven,
3) rerun one compile gate.
